# CookieClaude

レイトン風シナリオアドベンチャー『クッキーとクロードのナゾ解き事件簿』。

- 遊びかたと画面の一覧は `README.md`
- **ゲーム上の概念（エリア・シーン・出口）と中身の足しかたは
  `docs/DEVELOPMENT.md`**。ここを読まずにデータ構造をいじらないこと
- 開発者モードの使いかたは `docs/DEV-MODE.md`（手を動かす手順つき）

## 作業のながれ

**作業が完了したら、毎回 `main` へマージすること。** ユーザーからの指示による
恒久的な取り決めなので、そのつど確認をとる必要はない。

1. 作業ブランチで開発し、コミットする
2. 作業ブランチを `origin` にプッシュする
3. `main` にマージして `origin/main` にプッシュする
   （履歴を一直線に保つため、可能なら `git merge --ff-only`）
4. 作業ブランチは残しておく（消さない）

プルリクエストは、ユーザーが明示的に求めたときだけ作る。

## 完了とみなす条件

`main` にマージする前に、次を必ず通しておく。

```bash
bun run typecheck   # tsc --noEmit
bun run build       # dist/ へのバンドル
```

画面まわりを変えたときは、実際にブラウザで起動して確認する
（`bun run dev` → Playwright + `/opt/pw-browsers/chromium`）。
コンソールエラーが出ていないこと、変更した画面が意図どおり描画されることを見る。

## 書きかたの決まり

- CSS は素の CSS のみ。フレームワークもプリプロセッサも入れない
- 絵は基本すべてインライン SVG で描く。**ただしシーンの背景だけは、
  開発者が `src/data/images.ts` に登録すれば任意の画像を使える**
  （遊ぶ人は差しかえられない）。素材そのものはリポジトリに置かない。
  画像を読めなかったときは、描いた背景に戻す
- 音声素材はリポジトリに置かない。BGM・効果音は Web Audio API でその場で作る
  （`src/audio/`）。ただし遊ぶ人が設定で選んだ曲は鳴らす（端末の中だけに残る）
- 画面のエフェクトも素材を使わず、canvas に手続きで描く（`src/effects/`）。
  差しこみ口は `<EffectLayer slot="..." />`。足すときは `registry.ts` に一行
- 開発者モード（`src/dev/`）は遊びの側から切り離しておく。
  `App` からは `<DevTools api={...} />` を置くだけに留める
- ゲームの外のホームページ（`src/sites/`）も同じ扱い。`src/data/` も
  `GameState` も見ない。**サイトごとに作りを変える**（動かない／動きが中身／
  頁を持つ）。実在のサイトを手本にするときは習作と分かる形にし、
  名前も中身も架空のものにする
- アカウント登録は画面だけの雛形。サーバーへは送らず、入力も保存しない
  （検査の規則は `src/state/account.ts` に分けておく）
- ナゾを指す語は「ナゾ」とカタカナで書く（謎・なぞは使わない）
- 画面に出る文言は大人向けの書き方にする。漢字で書ける語は漢字で書き、
  児童書のような分かち書き（語のあいだの空白）はしない
- コメント・コミットメッセージも日本語
- 型は `src/types.ts` に集約する
- 画面に出る文字は `LocalizedText`。ただの文字列も書けるので、
  訳ができたところだけ `{ ja, en }` にする。UI の文言は `src/i18n/ui.ts`
- 会話は `src/data/scenarios/` にエリアごとで置く。分かれ道は
  行に `label` を付けて `choices` から飛ばす
- ナゾは形式を散らす。同じ形（数値入力ばかりなど）に寄せない
- 中身（エリア・シーン・人物・会話・ナゾ・アイテム・フラグ・実績）は
  `src/data/` だけで完結させ、画面側に埋めこまない。足したら
  `src/data/registry.ts` の検査を通すこと（起動時にコンソールへ出る）
- フラグ（`src/data/flags.ts`）の値は保存しない。進行状況から毎回引き直す。
  条件は関数ではなく `needs` にデータで書き、判定は一か所だけに置く
  （実在しない id を検査が見つけられるようにするため）
- 実績（`src/data/achievements.ts`）は条件を持たない。フラグの組み合わせだけを書く
- 用語は **エリア（Area）** と **シーン（Scene）** で統一する。
  「場所」「街並み」「地点」といった言いかたを混ぜない。
  シーンは背景 1 枚、エリアはシーンを束ねる地図上の区画
